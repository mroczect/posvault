[package]
name = "posvault_auth"
version = "0.1.0"
edition = "2024"
description = "Authentication and RBAC for posvault"
license = "MIT"
repository = "https://github.com/mroczect/posvault"

[dependencies]
posvault_handler = { path = "../posvault_handler" }
libage_auth_handler = "0.2"
librage = "1.1"
zeroize = "1.9"
age_auth = "0.2.0"
age-credentials = "1.0.0"

[dev-dependencies]
hex = "0.4.3"
sha2 = "0.11.0"
